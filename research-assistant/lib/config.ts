/** Container tag used to namespace this example's documents in Supermemory. */
export const CONTAINER_TAG = "research";

/** Maximum number of agent steps (tool-call iterations) per chat turn. */
export const MAX_AGENT_STEPS = 10;
